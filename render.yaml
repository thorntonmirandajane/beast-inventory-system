databases:
  - name: beast-inventory-db
    plan: free
    databaseName: beast_inventory
    user: beast_user

services:
  - type: web
    name: beast-inventory-system
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build && npm run db:migrate:deploy
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: beast-inventory-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: SKIP_PRISMA_VERSION_CHECK
        value: "true"
